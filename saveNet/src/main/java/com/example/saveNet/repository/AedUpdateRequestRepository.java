package com.example.saveNet.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.example.saveNet.entity.AedUpdateRequest;

public interface AedUpdateRequestRepository extends JpaRepository<AedUpdateRequest, Long>{
    
}
